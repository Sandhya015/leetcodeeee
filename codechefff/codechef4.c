#include <stdio.h>

#include <stdio.h>

int main() {
    int maxSpeed;
    scanf("%d", &maxSpeed);

    if (maxSpeed <= 40) {
        printf("NO\n");
    } else {
        printf("YES\n");
    }

    return 0;
}
